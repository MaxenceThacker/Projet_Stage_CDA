<form method="post" action="{{ path('app_formateurs_delete', {'id': formateur.id}) }}" onsubmit="return confirm('Voulez-vous vraiment supprimer cet élément ??');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ formateur.id) }}">
    <button class="button">Supprimer</button>
</form>
