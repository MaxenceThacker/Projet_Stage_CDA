{% extends 'base.html.twig' %}

{% block title %}Formateurs{% endblock %}

{% block body %}
    <h1>Formateurs</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ formateur.id }}</td>
            </tr>
        </tbody>
    </table>

    <a class="button" href="{{ path('app_formateurs_index') }}">Retour à la liste</a>

    <a class="button" href="{{ path('app_formateurs_Editer', {'id': formateur.id}) }}">Editer</a>

    {{ include('formateurs/_delete_form.html.twig') }}
{% endblock %}
