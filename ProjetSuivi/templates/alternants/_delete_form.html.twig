<form method="post" action="{{ path('app_alternants_delete', {'id': alternant.id}) }}" onsubmit="return confirm('Voulez-vous vraiment supprimer cet élément ??');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ alternant.id) }}">
    <button class="button">Supprimer</button>
</form>
