<form method="post" action="{{ path('app_alternants_delete', {'id': alternant.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ alternant.id) }}">
    <button class="button">Supprimer</button>
</form>
