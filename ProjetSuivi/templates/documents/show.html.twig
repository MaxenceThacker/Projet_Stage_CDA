{% extends 'base.html.twig' %}

{% block title %}Documents{% endblock %}

{% block body %}
    <h1>Documents</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Identifiant du document</th>
                <td>{{ document.id }}</td>
            </tr>
            <tr>
                <th>Libelle du document</th>
                <td>{{ document.libelleDocument }}</td>
            </tr>
        </tbody>
    </table>

    <a class="button" href="{{ path('app_documents_index') }}">Retour à la liste</a>

    <a class="button" href="{{ path('app_documents_edit', {'id': document.id}) }}">Editer</a>

    {{ include('documents/_delete_form.html.twig') }}
{% endblock %}
