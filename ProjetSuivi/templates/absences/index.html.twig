{% extends 'base.html.twig' %}

{% block title %}Absences index{% endblock %}

{% block body %}
    <h1>Absences index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Date de l'absence</th>
                <th>Nombre d'heure d'absence</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for absence in absences %}
            <tr>
                <td>{{ absence.dateAbsence ? absence.dateAbsence|date('Y-m-d') : '' }}</td>
                <td>{{ absence.nbrHeureAbsence }}</td>
                <td>
                    <a class="button" href="{{ path('app_absences_Voir', {'id': absence.id}) }}">Voir</a>
                    <a class="button" href="{{ path('app_absences_Editer', {'id': absence.id}) }}">Editer</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="4">Aucun enregistrement trouvé</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a class="button" href="{{ path('app_absences_new') }}">Nouveau</a>
{% endblock %}
