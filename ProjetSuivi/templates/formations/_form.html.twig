{{ form_start(form) }}
    <div class="colonne">
        {{ form_widget(form) }}
    </div>
    <div>
        <button class="button">{{ button_label|default('Sauvegarder') }}</button>
    </div>
{{ form_end(form) }}
