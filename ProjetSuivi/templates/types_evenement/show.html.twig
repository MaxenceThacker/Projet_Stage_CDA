{% extends 'base.html.twig' %}

{% block title %}TypesEvenement{% endblock %}

{% block body %}
    <h1>TypesEvenement</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ types_evenement.id }}</td>
            </tr>
            <tr>
                <th>LibelleTypeEvenement</th>
                <td>{{ types_evenement.libelleTypeEvenement }}</td>
            </tr>
        </tbody>
    </table>

    <a class="button" href="{{ path('app_types_evenement_index') }}">Retour à la liste</a>

    <a class="button"href="{{ path('app_types_evenement_Editer', {'id': types_evenement.id}) }}">Editer</a>

    {{ include('types_evenement/_delete_form.html.twig') }}
{% endblock %}
