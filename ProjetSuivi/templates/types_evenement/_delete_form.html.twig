<form method="post" action="{{ path('app_types_evenement_delete', {'id': types_evenement.id}) }}" onsubmit="return confirm('Voulez-vous vraiment supprimer cet élément ??');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ types_evenement.id) }}">
    <button class="button">Supprimer</button>
</form>
