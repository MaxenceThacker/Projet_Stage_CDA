{% extends 'base.html.twig' %}

{% block title %}TypesEvenement index{% endblock %}

{% block body %}
    <h1>TypesEvenement index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>LibelleTypeEvenement</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for types_evenement in types_evenements %}
            <tr>
                <td>{{ types_evenement.id }}</td>
                <td>{{ types_evenement.libelleTypeEvenement }}</td>
                <td>
                    <a href="{{ path('app_types_evenement_show', {'id': types_evenement.id}) }}">show</a>
                    <a href="{{ path('app_types_evenement_edit', {'id': types_evenement.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_types_evenement_new') }}">Create new</a>
{% endblock %}
