<form method="post" action="{{ path('app_types_absence_delete', {'id': types_absence.id}) }}" onsubmit="return confirm('Voulez-vous vraiment supprimer cet élément ??');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ types_absence.id) }}">
    <button class="button">Supprimer</button>
</form>
