{% extends 'base.html.twig' %}

{% block title %}TypesAbsence{% endblock %}

{% block body %}
    <h1>TypesAbsence</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ types_absence.id }}</td>
            </tr>
            <tr>
                <th>LibelleTypeAbsence</th>
                <td>{{ types_absence.libelleTypeAbsence }}</td>
            </tr>
        </tbody>
    </table>

    <a class="button" href="{{ path('app_types_absence_index') }}">Retour à la liste</a>

    <a class="button" href="{{ path('app_types_absence_Editer', {'id': types_absence.id}) }}">Editer</a>

    {{ include('types_absence/_delete_form.html.twig') }}
{% endblock %}
