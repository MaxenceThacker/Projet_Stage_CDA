{% extends 'base.html.twig' %}

{% block title %}Tuteurs{% endblock %}

{% block body %}
    <h1>Tuteurs</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ tuteur.id }}</td>
            </tr>
        </tbody>
    </table>

    <a class="button" href="{{ path('app_tuteurs_index') }}">Retour à la liste</a>

    <a class="button" href="{{ path('app_tuteurs_edit', {'id': tuteur.id}) }}">Editer</a>

    {{ include('tuteurs/_delete_form.html.twig') }}
{% endblock %}
