{% extends 'base.html.twig' %}

{% block title %}Tuteurs index{% endblock %}

{% block body %}
    <h1>Tuteurs index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Nom</th>
                <th>Prénom</th>
                <th>Date de naissance</th>
                <th>Numéro de Téléphone</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for tuteur in tuteurs %}
            <tr>
                <td>{{ user.nomUser }}</td>
                <td>{{ user.prenomUser }}</td>
                <td>{{ user.ddnUser ? user.ddnUser|date('Y-m-d') : '' }}</td>
                <td>{{ user.numTelUser }}</td>
                <td>
                    <a href="{{ path('app_tuteurs_show', {'id': tuteur.id}) }}">show</a>
                    <a href="{{ path('app_tuteurs_edit', {'id': tuteur.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="2">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_tuteurs_new') }}">Create new</a>
{% endblock %}
