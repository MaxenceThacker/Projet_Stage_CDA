{% extends 'base.html.twig' %}

{% block title %}Formateurs{% endblock %}

{% block body %}
    <h1>Formateurs</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ formateur.id }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_formateurs_index') }}">back to list</a>

    <a href="{{ path('app_formateurs_edit', {'id': formateur.id}) }}">edit</a>

    {{ include('formateurs/_delete_form.html.twig') }}
{% endblock %}
