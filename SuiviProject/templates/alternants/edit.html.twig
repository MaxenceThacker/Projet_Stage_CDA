{% extends 'base.html.twig' %}

{% block title %}Edit Alternants{% endblock %}

{% block body %}
    <h1>Edit Alternants</h1>

    {{ include('alternants/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('app_alternants_index') }}">back to list</a>

    {{ include('alternants/_delete_form.html.twig') }}
{% endblock %}
